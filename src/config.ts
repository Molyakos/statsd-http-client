export type Config = {
    scheme?: string
    address?: string
    host?: string
    port?: number
    prefix?: string
    suffix?: string
    useDebounce?: boolean
    debounceTime?: number
    token?: string
}
